import { Canvas, Controls, Meta } from '@storybook/addon-docs';
import * as LabelStories from './Label.stories';

<Meta of={LabelStories} title="Atoms/Labels" />

# Labels
Labels are used in a range of different situations from a simple text element included in a control through to the text of a question.
<Canvas of={LabelStories.PreLabel} />
<Controls of={LabelStories.PreLabel} />