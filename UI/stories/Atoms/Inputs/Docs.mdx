import { Canvas, Controls, Meta } from '@storybook/addon-docs';
import * as InputStories from './Input.stories';

<Meta of={InputStories} title="Atoms/Inputs" />

# Inputs
An input is used in almost all question types, its the way that the data is passed between Dimensions and the form. Some of the types can be exposed to the respondent to allow them to make an entry, the others are hidden with the response type interacting to generate the relevant content.

## text
<Canvas of={InputStories.Singleline} />
<Controls of={InputStories.Singleline} />

## number
<Canvas of={InputStories.SinglelineNumber} />
<Controls of={InputStories.SinglelineNumber} />

## date
<Canvas of={InputStories.SinglelineDate} />
<Controls of={InputStories.SinglelineDate} />

## range
<Canvas of={InputStories.SinglelineRange} />
<Controls of={InputStories.SinglelineRange} />