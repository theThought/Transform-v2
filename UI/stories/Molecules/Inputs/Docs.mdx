import { Meta } from '@storybook/blocks';

<Meta title="Inputs/Single Line" />
#m-singleline
When a single line input is presented to the user for completion.  The single line input types current supported as a direct method of input are:
- text
- number

Other singleline input types are used in the background to support other question types, but are not supported as a front-end UI component
