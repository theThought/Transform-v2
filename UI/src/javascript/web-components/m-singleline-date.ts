import MSingleline from './m-singleline';

export default class MSinglelineDate extends MSingleline {
    constructor() {
        super();
    }

    public connectedCallback(): void {
        super.connectedCallback();
    }
}
