import OQuestion from './o-question';

export default class OQuestionResponse extends OQuestion {
    constructor() {
        super();
        /* TODO: Should we extend 'OQuestion' or 'HTMLElement'? */
    }
}
